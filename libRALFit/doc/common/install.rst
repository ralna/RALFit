============
Installation
============

Building the library
--------------------
From the ``RALFit/libRALFit/`` directory, issue the commands:

.. code::

   mkdir build
   cd build
   cmake ..
   make

