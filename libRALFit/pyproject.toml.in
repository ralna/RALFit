[build-system]
requires = ['numpy', 'setuptools', 'wheel']
build-backend = 'setuptools.build_meta'

[project]
name = 'RalNLLS'
description = 'RALFit a nonlinear least square solver'
version = '@PROJECT_VERSION@'
dependencies = ['numpy']

